// Restricted task types — must match the `task` check constraint in
// supabase_schema.sql. Shared by UnitPopup (add-task dropdown) and
// StatsPanel (task search filter).
export const TASK_OPTIONS = ['bathroom', 'kitchen', 'tub', 'cabinet', 'waterline', 'floor', 'beam']
